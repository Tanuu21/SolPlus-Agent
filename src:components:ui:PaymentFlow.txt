import React from 'react';
import { motion } from 'framer-motion';

export const PaymentFlow: React.FC = () => {
  return (
    <div className="flex flex-col md:flex-row justify-between items-center gap-4 py-8 relative">
      <div className="absolute inset-x-0 top-1/2 h-[2px] bg-gradient-to-r from-purple-500 via-cyan-500 to-emerald-500 opacity-20 hidden md:block" />
      
      {[
        { step: '1', title: 'SAP Node Discovery', desc: 'Synapse Engine mapping query target parameters via mainnet registry structures.', color: 'border-purple-500/40 text-purple-400' },
        { step: '2', title: 'Ace AI Structuring', desc: 'Concurrent assessment of liquidity metrics and directional token metadata patterns.', color: 'border-cyan-500/40 text-cyan-400' },
        { step: '3', title: 'x402 Atomic Clearing', desc: 'Simultaneous internal asset distribution, routing execution rewards directly to secure escrow storage.', color: 'border-emerald-500/40 text-emerald-400' }
      ].map((node, i) => (
        <motion.div 
          key={i}
          initial={{ opacity: 0, y: 15 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: i * 0.2 }}
          className="z-10 bg-[#0B0B0B] border border-white/[0.06] p-5 rounded-xl w-full md:w-[30%] shadow-xl relative group"
        >
          <div className={`absolute top-3 right-3 text-xs px-2 py-0.5 rounded-full border bg-black ${node.color} font-mono`}>
            Step 0{node.step}
          </div>
          <h4 className="text-white font-medium text-sm mb-1 mt-2 tracking-wide group-hover:text-accentCyan transition-colors">
            {node.title}
          </h4>
          <p className="text-gray-400 text-xs leading-relaxed">{node.desc}</p>
        </motion.div>
      ))}
    </div>
  );
};
